Query the Dorchester engine for deep, targeted repo intelligence: structural analysis, hotspots, correlations, blast-radius impact, and an execution brief scoped to a specific objective. Use this instead of manually reading many files when you need to understand how a change to `target` will ripple through the codebase.

Billed per line of code actually (re-)analyzed for this query, deducted from your OpenKrak credit balance (tops up $2/day, rolls over — never force-reset).

This is OpenKrak's signature feature: a deterministic static-analysis engine that does the heavy repo-reading so the model doesn't have to burn its own context budget doing it manually.
